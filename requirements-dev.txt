# Dev-only: the e2e test suite (make test). The runtime scripts stay
# stdlib-only; pygments (syntax highlighting) and wenmode (markdown rendering in
# the mirror, core/mdrender.py) are the two optional runtime niceties, pinned
# here so renderer goldens/tests are reproducible.
# Common lint and test tools come from the same policy used by external packages.
-e ./packages/dev-tools
# Extension tests start a private host from an installed executable through the shared kit.
-e ./packages/extension-testkit
# Offline test wheel builds use the SDK's standard build backend and wheel packer.
setuptools>=77
wheel>=0.45,<1
# Gherkin scenarios for the LIVE-harness suite (tests/e2e, `make test-drift`).
# pytest-bdd rather than behave because it compiles a .feature into ordinary
# pytest tests: that suite is all fixtures (a spawned daemon, a pseudo-terminal,
# a workspace), and behave's own runner would reach none of them. `make test`
# ignores tests/e2e, so the hermetic suite still runs without this installed.
pytest-bdd>=8.1
# Browser-origin Gherkin cases use the same pytest fixtures and real daemon as
# the live harness suite. The plug-in supplies typed browser pages; Chrome is
# selected by the opt-in browser runner, so no recorded frontend server is used.
pytest-playwright==0.9.0
playwright==1.62.0
# A normal embedded TrueType font makes image-attachment checks test the image
# path, not the harness model's ability to read a custom block font.
Pillow>=10.1
# Typed HTTP transport for the live application client.
httpx>=0.28
# The terminal emulator behind terminal/impl/pty — the headless terminal that
# suite drives its harnesses through. A pty gives paint operations; pyte adds
# them up into the screen `read_screen` is asked for. Dev-only because pinning
# BAQYLAU_TERMINAL=pty is what needs it, and nothing does that in production.
pyte>=0.8
pygments>=2.17
wenmode>=0.9
# Exact common pins are in packages/dev-tools/src/baqylau_dev/policy/requirements.txt.
# Stubs for the two typed third-party imports. Without them mypy reports the
# imports themselves as untyped, which strict mode treats as an error.
# (AppKit ships neither stubs nor py.typed and is declared in mypy.ini instead.)
types-PyYAML==6.0.12.20260815
types-Pygments==2.20.0.20260728
types-psutil==7.2.2.20260518
types-jsonschema==4.26.0.20260518
