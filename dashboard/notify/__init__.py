# dashboard/notify/ — the cross-session notification watcher: presence
# signals (presence.py) + the tab-diff Notifier / toast / off-device alert
# fan-out (notifier.py). docs/dashboard.md, *Telegram alerts* / *Web push*.
