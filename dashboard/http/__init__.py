# dashboard/http/ — the web dashboard's HTTP layer: the plumbing base
# (base.py), the GET read plane (get.py), the POST control plane (post.py),
# canonical resource and SSE delivery (canonical.py), composed into Handler +
# serve() (handler.py).
