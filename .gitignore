__pycache__/
*.pyc
.venv/
claude-tab-status.log
# audit system artifacts — the DB lives in ~/.claude/baqylau-audit by default, but
# keep it out of git even if CLAUDE_AUDIT_DIR is pointed into the repo
baqylau-audit/
*.db
*.db-wal
*.db-shm
spool.jsonl
# stray live-fg streaming sentinels (tee side-files .done markers landing in cwd)
*.done
.DS_Store
dashboard/frontend/node_modules/
dashboard/frontend/coverage/
dashboard/frontend/playwright-report/
dashboard/frontend/test-results/
dashboard/static/build/

# Keep the shared GitHub Projects skill visible through Claude Code's skill path.
!.claude/
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/github-projects
