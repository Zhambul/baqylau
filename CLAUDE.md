# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

**baqylau** (Kazakh *бақылау*, "observation"; formerly *claude-kitty*) — a kitty-terminal integration for Claude Code, built entirely out of Claude Code **hooks** plus detached background processes. Three user-facing features:

1. **Tab colors** (`claude-tab-status.py`) — the kitty tab color reflects the session state (grey idle · magenta busy · blue running/awaiting · red asking-you · green your-turn) via `kitten @ set-tab-color` over the socket in `$KITTY_LISTEN_ON`.
2. **Command mirror pane** (`claude-split.py` + `claude-mirror.py`) — a right-side vertical split showing every command, file op, subagent, teammate, monitor, and codex run as colored streaming blocks — fg/bg command blocks carry clickable ` ⧉cmd ⧉out` copy links, and Read/Update/Write one-liners click-to-EXPAND their content in place — highlighted code for Read/Write, a line-numbered soft-red/green diff for Update; second click collapses (both via OSC 8 hyperlinks → `claude-copy.py` via `~/.config/kitty/open-actions.conf`, docs/wiring.md / docs/click-to-view.md) — plus a 5-row scoreboard window (`claude-scorebar.py`) underneath (session id · ✉ message census · ▪ activity (cmds + time) · Σ token breakdown + cost · files + ± line-diff + tools).
3. **Audit trail** (`core/audit.py`, CLI `bin/claude-audit.py`) — always-on SQLite recording of every hook event, tab transition, slot claim, stream lifecycle, paint op, and swallowed exception, at `~/.claude/baqylau-audit/audit.db`.
4. **Web dashboard** (`dashboard/` + `bin/claude-dashboard.py`, docs/dashboard.md) — a localhost web UI (127.0.0.1:8377, `CLAUDE_DASH_PORT` overrides) over EVERY session, live and parked: the mirror stream rendered to HTML (`dashboard/opshtml.py` — html-escape is the `neutralize()` analog; ⧉ copy / click-to-view work via the same `core/copy.collect` + kv stashes; MAIN-AGENT-ONLY, unlike the terminal mirror: agent/teammate/secondary-codex ops carry a producer-source stamp — `core/ops.py` `src`, ambient per-process `set_src`/`$CLAUDE_OPS_SRC` — and `op_items` drops them, EXCEPT a subagent's `⇢ prompt` + `⇠ result` blocks, which the substream stamps `web=1` to surface in the main mirror alongside the lead's launch header; the rest of the agent's detail lives in the drill-down), per-agent activity drill-down (`plugins.activity()` timelines), ctx-saturation bars (own row, account-limit-bar styling) on the session cards / session header / agent cards, git branch+worktree chips with a dirty `*` (`git_info` — .git-file reads for branch/worktree; the dirty flag is the ONE sanctioned `git status` subprocess, TTL-cached per cwd; a linked worktree also carries `root`, its owning main checkout — the list page groups sessions by the server's `group_dir` (the session's FROZEN original cwd — audit `sessions.start_cwd`, set once at SessionStart and never re-stamped, added by `audit._migrate` — resolved to its worktree owner), so worktree sessions aggregate under their project AND a mid-session `cd` can't move a card between groups, docs/dashboard.md *Grouping and titles*; a group header's `✕` HIDES that directory from the crowded list — non-destructive (sessions keep running, tabs/toasts still fire), it re-appears the instant a session started after the hide-time shows up there (`POST /api/dirs/hide` stamps `time.time()` into the `hidden-dirs` key of the same durable `dashboard/prefs.py` store; the re-appear predicate is client-side over each row's `started_at`, docs/dashboard.md *Hidden directories*) — a directory with an ACTIVE (live) session can't be hidden: the ✕ is disabled and the POST 409s (`dir_live_sessions`), and a hidden dir that gains a live session re-shows at once (the `dirHidden` `live` short-circuit) (the path-keyed `plugins.context()` fan-out — `transcript.context_probe` bounded-tail probe over the last assistant usage, docs/dashboard.md *Context saturation*), cross-session toast/OS notifications when a tab goes red (asking you) or green (done) — a 1s `tab_states()` diff watcher over SSE — PLUS a DEFERRED off-device Telegram alert on the same transitions, reusing the global `notify` skill: armed on the transition, sent only if the tab is STILL red/green after a grace window (you didn't react — the arm is cancelled the moment the state moves off, the session ends, an unsent web composer draft exists, OR — for an `asking` arm — the on-screen AskUserQuestion dialog region CHANGES, i.e. you're typing a free-text answer / toggling a selection AT THE TERMINAL, the one trace terminal answering leaves since it moves neither the tab off red nor the transcript: `askdialog.region` diff over `Frontend.get_text`, a `notify-suppress` state_files row) and the session isn't muted, browser-independent; per-session opt-out via the 🔔/🔕 header toggle (`POST /api/session/<sid>/notify` → the `notify-muted` map in `dashboard/prefs.py`), the alert's deep link uses the PUBLIC proxied origin `CLAUDE_DASH_PUBLIC_URL` (default `https://baqylau.zhambyl.top`), not the 127.0.0.1 bind (it lands on a phone); env knobs `CLAUDE_DASH_NOTIFY_DELAY_S`/`_TELEGRAM`/`_CMD`/`CLAUDE_DASH_PUBLIC_URL`, audited as `telegram-notify`/`notify-mute`/`notify-suppress` state_files rows, docs/dashboard.md *Telegram alerts* — and a full control plane (message composer — on a PARKED session it becomes "resume & send": the message rides a `--resume` relaunch's argv through the existing web-launch path, docs/dashboard.md *Resume & send*; the composer's UNSENT text is persisted per-session in the `composer-draft` kv — written on every edit, cleared on send — so a device switch / reload / return-to-session restores the half-typed message (SSE `composer-draft` event + `origin`-echo suppression, the *Web ask* draft machinery reused, docs/dashboard.md *Web composer draft*) — the composer also MIRRORS Claude Code's greyish input-box "suggested answer" (the ghost text the TUI pre-fills when a turn settles): screen-scraped LIVE (no hook fires for it; `dashboard/suggestion.py` `parse`/`probe` reads the faint-SGR `\x1b[22;2m` input line between the grey divider rules off an ANSI `get_text(win, ansi=True)` capture, sibling of askdialog/plandialog), pushed on the `suggestion` SSE event (gated on a settled tab + no pending modal + empty draft, ephemeral/live-only — no kv), shown as the textarea's grey placeholder and accepted with `→`/Tab into the box (`applySuggestion`/`syncSuggestion`; a MIRROR + client-side accept, nothing typed back to the TUI), docs/dashboard.md *Web ghost suggestion* — ATTACHMENTS on the composer AND the new-session prompt (paste a screenshot, drag-drop, or the 📎 picker): the bytes upload to `POST /api/upload` (JSON+base64, cap raised to `UPLOAD_MAX`), the server stages them under `core.paths.UPLOADS_DIR` (`~/.claude/baqylau-uploads/<sid>/`, outside any repo working tree) as a `web-upload` state_files row, and the composer injects each as a leading `@path` mention (`_with_attachments`) that rides the existing `paste_text`/launch-argv delivery — Claude Code itself resolves the mention and attaches the file, the ONE native image path since the CLI has no image flag (`_attachment_paths` admits only paths inside `UPLOADS_DIR`), docs/dashboard.md *Web attachments* — interrupt, mid-turn cancel-edit, close, rename, ⇆ migrate (the manual trigger of the relimit account migration — no confirm, no % ceiling, docs/relimit.md *Manual migrate*) — the ✎ header button appends the `agent-name` naming record through its single owner `transcript.set_session_title` (the ONE control-plane session-state write, docs/dashboard.md *Web rename*) + retitles a live tab via the new `Frontend.set_tab_title` (sticky), works live AND parked — quick commands — a second scoreboard action row typing the TUI's own `/compact` + `/model` + `/effort` (closed vocabulary, `post_command`, docs/dashboard.md *Web quick commands*), and FULL rewind: pick any prompt bubble + restore mode and `dashboard/rewindmenu.py` drives Claude Code's own checkpoint menu with screen-verified key events — docs/dashboard.md *Web rewind*; and FULL AskUserQuestion support: the ask card mirrors the TUI dialog — options/multiSelect/free-text/chat-about-this — via the `ask-pending` kv stash (`plugins/claude_code/ask_fmt.py`) and `dashboard/askdialog.py` driving the real dialog, docs/dashboard.md *Web ask*; and FULL plan-mode support: the plan card renders the pending ExitPlanMode plan + the dialog's own decision options fetched from the live screen, `dashboard/plandialog.py` — approve/feedback/dismiss, docs/dashboard.md *Web plan mode*; and a PINNED TASKS CARD at the top of the mirror tab — the session's native TaskCreate/TaskUpdate list, completed rows struck-through/dimmed, in_progress accented — fed by the `tasks` kv `task_fmt.py` re-snapshots from Claude Code's on-disk task dir on every task-touching hook (the dir is wiped at session end, so the kv is what parked sessions show; a status flip fires NO dedicated hook — its PostToolUse is the only refresh signal), docs/dashboard.md *Web tasks*; and a PINNED GOAL CARD above it — Claude Code's active `/goal` (the 2.1.139+ autonomous-completion built-in), amber while working and green ✓ once the checker reports it met — detected READ-SIDE with NO hook (none fires for `/goal`): `transcript.goal_probe` tail-scans the transcript for the last `goal_status` attachment (the goal lives in the transcript, restored on resume), the path-keyed `plugins.goal()` fan-out (sibling of `plugins.context()`), `session_goal` memo + `goal` SSE event, adds no audit rows (same as ctx saturation), docs/dashboard.md *Web goal*; and a MEMORY TAB (after jobs) over the memory-wiki notes (`~/wiki/01`) a session touched — read/write/update labelled, team-wide (main agent AND subagents), fed by the per-session `memory` kv the single-owner `plugins/claude_code/memory.py` stashes on every file op under the root (its 🧠 `MARK` + `mem` op tag also give memory ops their own mirror stream-filter kind); SCOPED to one project — the feature (marker + tab) activates only for sessions inside `~/code/01/aggregator-adapters` (`memory.in_scope`; the server serves `memory_scope` and the tab is hidden off-scope), since the shared wiki covers all of `code/01` but only that project opts in; clicking a note renders its markdown to safe HTML with clickable `[[wikilinks]]` (whole-vault resolution) + a backlinks section (`dashboard/notehtml.py` over `opshtml.md_html`), docs/dashboard.md *Memory tab*; and MIC DICTATION on the composer + the new-session prompt — Deepgram Nova-3 streaming, live interim results spliced into the textarea; the server only mints a ~30s grant JWT (`dashboard/dictate.py`, `POST /api/dictate/token`, key at `~/.config/deepgram/api-key`; keyterms LAYERED project-first — each applicable `.claude/deepgram-keyterms` via the `plugins.config_dirs` walk, then the global keyterms file alongside the key) and the browser talks wss to Deepgram DIRECTLY (the stdlib server can't speak WS and never sees audio), docs/dashboard.md *Web dictation*; and the new-session form's last-used directory/model/effort, moved off per-browser `localStorage` into the DURABLE GLOBAL prefs store `dashboard/prefs.py` — a tiny kv table at `core.paths.DASH_PREFS_DB` (~/.claude), the one piece of dashboard state that is cross-session/cross-device rather than per-session, `GET`/`POST /api/ns-prefs`, docs/dashboard.md *New-session prefs*); and a FRONTEND AUDIT channel (`clog` → `POST /api/clientlog` → `web-client` state_files rows, docs/dashboard.md *Frontend audit (clientlog)*) — the browser reporting what the SERVER can't see: the transport lifecycle of every tagged control POST (`<gesture>.begin`/`.ok`/`.fail` with timing + status, auto-logged by the instrumented `postJSON` spine — close/send/command/interrupt/rename/migrate/rewind/rewind-to/answer/plan/new/resume-send), SSE up/down transitions (`sse.open`/`sse.drop` — the connection-pool evidence), uncaught JS errors (`js.error`/`js.reject`), and a per-load `boot` record (origin `127.0.0.1` vs the tunnel), each batch stamped with a `connInfo()` snapshot — so a control request the browser TRIED but that never reached a handler (a tunnel-dropped `/stop`: a `close.begin` with no `.ok`/`.fail` — the whole "still not closing" bug class, invisible in the server-side audit) is now attributable from the DB; the close is on the plain-`fetch` channel (`navigator.sendBeacon` REGRESSED it — queued-then-dropped by the tunnel), docs/dashboard.md *Close via the plain-fetch channel*); and a STATS / INSIGHTS page (header 📊 → `#/stats`, `GET /api/stats` → `stats_payload` over `sessionapi.activity_stats`) — a GitHub-Insights-inspired cross-session, over-time view (the list page only shows CURRENT sessions): a contribution heatmap (sessions/day, 5 self-normalized buckets), a day×hour punch card (bubble radius ∝ sessions), a period-toggled Pulse KPI strip (sessions/active/ended/tokens/cost/errors + top-projects bars), and per-project cards (90-day sparkline + counters, grouped by `_group_dir` like the list) — all SERVER-computed from a few indexed `GROUP BY`s over the audit `sessions`/`otel`/`errors` tables (the JS only renders hand-rolled SVG, no chart library), memo-cached `STATS_TTL_S` (wall-clock), read-only so it adds NO audit rows (like `accounts_payload`/ctx/goal), docs/dashboard.md *Stats / Insights*). A CONSUMER tier above core/plugins (imports both, nothing imports it back); read-only server (`ThreadingHTTPServer`, per-request `mode=ro`, singleton pid-lock on `paths.DASH_DB` + port bind, `stream_lifecycle` kind `dashboard`).

There is no build system or package manifest. Scripts are invoked directly by hooks wired in `~/.claude/settings.json` (the hook table is in docs/wiring.md — the settings file itself is *not* in this repo). Python scripts target the system `python3`; only `pygments` is an (optional, probed-for) runtime dependency. The e2e test suite lives in `tests/` (`make test`, dev-only deps in `requirements-dev.txt` — see docs/testing.md); it drives the real hook scripts as subprocesses against per-test temp dirs and a fake `kitten` recorder.

**Layout (core / plugins / frontends — docs/architecture.md).** `core/` is the tool- and terminal-agnostic runtime; `plugins/<tool>/` adapts one agent tool each (`claude_code` — a host; `codex` — a secondary source inside a Claude session AND a standalone host on its own, via codex's native SessionStart hook → `claude-codex-session.py`), registered in `plugins/__init__.py` (`all_plugins` / `on_session_start` / `census` / `activity` — the last is the drill-down timeline fan-out behind `core/sessionapi.py`, docs/sessionapi.md); both hosts drive the shared `core/hostpane.py` pane lifecycle; a third plugin `plugins/otel/` is NOT an agent tool but a cross-cutting subsystem — a per-machine singleton OpenTelemetry OTLP receiver (`claude-otlp-receiver.py`, spawned at SessionStart when `CLAUDE_CODE_ENABLE_TELEMETRY=1`) that is the AUTHORITATIVE source of the scoreboard's token/cost counters (it captures Claude Code's hidden `auxiliary` agents that transcript-folding never could; the transcript fold survives only as a SessionEnd fallback). Raw datapoints land in the audit `otel` table; `frontends/` adapts one terminal each behind the `Frontend` interface (`frontends.get()`; kitty is the only implementation, `$CLAUDE_FRONTEND` overrides). Dependency rule: core imports only core; frontends at most core; plugins import core + frontends, never each other; `dashboard/` (the web-dashboard consumer tier, docs/dashboard.md) sits above all — it imports core + the plugins registry root + frontends (the last for its control plane: the write endpoints reach the terminal through `frontends.get()`), and nothing imports it back except its bin/ entry and tests. The `bin/` directory holds EVERY executable entry point (`bin/claude-*.py` — hook entries, renderers, streamers, the audit CLI `bin/claude-audit.py`, plus `bin/retarget-python.py`); their FILENAMES are load-bearing (they name the audit's handler/script vocabulary) — never rename an entry (moving is fine, the vocabulary is the basename); put implementation in the packages, and spawn siblings via `core/paths.py`'s `BIN`, never a re-derived path. **Naming convention (deliberate, not drift):** hyphenated `claude-*.py` = executable entry, un-importable by design; underscored `*.py` inside `core/`/`plugins/`/`frontends/` = importable module. The repo root contains no Python files. **All hook events are wired (in `~/.claude/settings.json`, outside the repo) to ONE dispatcher entry, `claude-hook.py` → `plugins/claude_code/dispatch.py`** — except `WorktreeCreate`/`WorktreeRemove`, which must stay UNWIRED (delegating hooks: registering one overrides Claude Code's native worktree creation and must print the worktree path; the dispatcher's silent exit-0 broke `EnterWorktree` — docs/wiring.md), which reads the payload once and fans out IN-PROCESS to the tab dispatch, the matcher-selected formatter, and the universal audit subscriber (matcher routing lives in `dispatch.py`'s `_plan()`; docs/wiring.md). Because every subsystem runs under argv[0]=`claude-hook.py`, the dispatcher stamps `audit.set_handler("claude-<x>.py")` around each call so `hook_events.handler`/`errors.script` keep their per-entry vocabulary (argv[0] is no longer the sole source — an explicit override is). The per-script entry shims (`bin/claude-cmd-fmt.py` …) still exist and still run standalone (the e2e tests drive them directly). The historical root-level compat shims (`claude_state.py`, `claude_ops.py`, …) are DELETED — import the package modules directly (`core.state`, `core.ops`, `plugins.claude_code.hookkit`, …).

## Commands

```sh
# Audit CLI — the primary debugging tool (run from repo root)
python3 bin/claude-audit.py sessions            # recent sessions
python3 bin/claude-audit.py anomalies <sid>     # canned queries for known bug signatures — start here
python3 bin/claude-audit.py errors    <sid>     # swallowed exceptions, full tracebacks
python3 bin/claude-audit.py timeline  <sid>     # merged chronological story of a session
python3 bin/claude-audit.py otel      <sid>     # OTEL cost/token breakdown by query_source (main/subagent/auxiliary)
python3 bin/claude-audit.py sql "<query>"       # free-form read-only SQL (sql-write for fixups)

# Manual smoke test — cycle the tab colors (~3s each)
for s in idle thinking working executing awaiting-bg awaiting-command awaiting-response; do
  ./bin/claude-tab-status.py "$s"; ping -c 4 127.0.0.1 >/dev/null
done
./bin/claude-tab-status.py clear

# Mirror pane controls
./bin/claude-split.py toggle|grow|shrink|reset|setpct <N>

# Web dashboard (docs/dashboard.md)
./bin/claude-dashboard.py            # start if needed + open the browser
./bin/claude-dashboard.py serve|start|stop|status

# E2E test suite (hermetic — fake kitten, per-test tmp dirs; see docs/testing.md)
make test        # the whole hermetic suite (parallel by default, pytest-xdist)
make test-seq    # same, sequential (debugging / no xdist)
make test-all    # + the opt-in real-kitty smoke tests

# Lint (ruff, pinned in requirements-dev.txt; ruff.toml encodes docs/styleguide.md; CI-enforced)
make lint        # must stay clean
make lint-fix    # apply safe auto-fixes
```

To debug a reported session bug, prefer the **`audit-debug` skill** (`.claude/skills/audit-debug/SKILL.md`) — it walks the triage order (anomalies → errors → timeline → targeted SQL) and documents the full audit schema.

Script edits take effect immediately (hooks re-exec them). Only `kitty.conf` changes (e.g. `listen_on`) need a full kitty restart, and a renderer/interpreter change needs a mirror toggle off/on.

**The web dashboard is the ONE exception — it does NOT hot-reload.** It is a long-running singleton server process (`bin/claude-dashboard.py serve`) started from the MAIN checkout, so a dashboard change is invisible until BOTH happen: the new code is on disk in the main checkout (merged to `main` and the checkout fast-forwarded — not just pushed to `origin/main` from a worktree) AND the server is restarted. Concretely:
- **Backend** (`dashboard/*.py`, or any `core`/`plugins`/`frontends` module the server imports): the code is loaded into the live process's memory — it keeps running the OLD code until `./bin/claude-dashboard.py stop && ./bin/claude-dashboard.py start` (verify with `status`; confirm with `curl -s http://127.0.0.1:8377/api/...`).
- **Frontend** (`dashboard/static/app.js` / `style.css` / `index.html`): served as static files, so a restart serves fresh bytes — but the BROWSER caches the old ones. The restart bumps the server's `BOOT_ID`, so an open page's SSE reconnect toasts "dashboard updated — refresh"; the user must hard-reload (Cmd+Shift+R) to pick up new JS/CSS.
So: after ANY dashboard front- or back-end change, restarting the dashboard is part of finishing the work (see the worktree-merge section below), and the user is told to hard-reload the browser.

## Architecture

**Producer/renderer split via paint ops.** Hook handlers and tailers never print to the pane. They append width-INDEPENDENT **paint ops** (`rule`/`label`/`code`/`gut`/`line`, built by `core/ops.py`) as rows of the per-session state DB's `ops` table (`/tmp/claude-mirror-<session_id>.log.state.db` — the `.log` path is only the KEY everything derives from; no log file exists). The single renderer `claude-mirror.py`, running inside the pane, polls new rows by id, paints ops at the live pane width and re-renders everything on SIGWINCH so content reflows. Consequence: anything width-dependent (wrapping, gutters, dividers) belongs in the renderer; anything width-independent (syntax highlighting, one-liner reflow) runs once at op creation.

**Process model.** ~20 short-lived hook processes plus detached long-lived tailers/watchers, coordinating through SQLite: a per-session state DB (`core/state.py` — ops, scoreboard, slots, hand-offs; the live DB is in `/tmp` but at SessionEnd it is MOVED to the durable park `~/.claude/baqylau-mirror-history/` — `core/paths.py` `HISTORY_DIR`/`parked_db` — and restored on resume, so a resumed session replays its mirror history even across a machine reboot that wipes `/tmp`), a global window-keyed tab DB (`/tmp/claude-kitty-tab.db` — tab colour + watcher pid locks), and `~/.claude/kitty-mirror.db` (remembered pane sizes). The only plain files left are what physics demands: the fg tee'd `.out` streams + `.done` sentinels (written by the rewritten command itself). Everything is Python; `claude-tab-status.py` reads the state/tab DBs read-only (`mode=ro`, so a probe can never create a DB whose existence is a liveness signal):
- `claude-cmd-pre.py` (PreToolUse Bash) rewrites foreground commands via `updatedInput` to tee output into a side file so it streams live; `claude-cmd-fmt.py` (PostToolUse) hands the real outcome to the tailer via a take-once hand-off record in the state DB. A **subagent's** foreground command is teed the same way (keyed by `tool_use_id`), but `claude-substream.py` spawns its tailer and hands off the outcome; gated by `CLAUDE_MIRROR_LIVE_FG_SUB`.
- `claude-stream.py` tails background/monitor/fg output files; `claude-substream.py` tails a subagent/teammate transcript (`subagents/agent-<id>.jsonl`) — the only in-order source of its prompt/messages/tools/result; `claude-codex-watch.py` (one per session) discovers every codex run from two global directories and spawns `claude-codex-stream.py` per run.
- Detached processes are spawned with `start_new_session=True` — spawning from a hook with bash `&` leaves them in the hook's process group, which Claude Code waits to drain (this hung SessionStart once; see `claude-codex-launch.py`).

**Slot rows** (`core/slots.py`, the state DB's `live` table) do double duty: they assign each concurrent stream a palette color (5-color palettes per kind: bg/monitor/subagent/teammate/codex) AND they are the tab tracker's liveness signal — `claude-tab-status.py stop` keeps the tab blue exactly while a live-pid row (kind `bg`/`monitor`/`fg`/`sub.pid`) exists. Rows store the owner pid and are liveness-checked (`core.state.pid_alive` — the ONE probe, where EPERM = exists-but-foreign-owned = alive) before being trusted; stale ones are stolen.

**Core modules (`core/`):** `ops.py` (paint ops, `emit`, scoreboard `bump()`, plus the shared vocabulary producers must not re-encode: the semantic colour table `SLATE/ORANGE/RED/GREEN/YELLOW/BLUE/AMBER`, `fmt_dur`, `kfmt`, `fmt_usd`; `split_tokens()` — the ONE usage-fields → `tk_*` mapping every Σ-row producer calls (tk_in subtracts cache creation from the input figure; per-site re-encoding is banned); `token_parts()` renders the `tk_in`/`tk_out`/`tk_read`/`tk_create` counters as the scorebar's Σ row — the sole token display, reconciling with `claude --resume`'s "Usage by model" total (the `▪` row dropped its billed-spend `tok` chip, though the `tokens` counter still backs the cost figure)), `state.py` (per-session RUNTIME state in SQLite at `/tmp/claude-mirror-<sid>.log.state.db` — the mirror's `ops` stream, scoreboard counters, team-message tracker, per-agent records incl. the stop flag + resume checkpoint, description queue, take-once hand-offs, the `live` slot table; also the transaction primitives every writer uses — `immediate()` BEGIN-IMMEDIATE context manager, `counter_add/set/get`, `transcript_fold` (the token-cursor transaction `accounting.bump_transcript` parses/prices into) — and `tab_state()`, the one sanctioned reader of the tab DB; load-bearing for behavior and deliberately separate from the audit DB; its file-existence is the session-alive signal watchers poll, probed via `parked()` — a bare exists check, never a connect, used by every tailer/watcher completion loop), `tabs.py` (the tab-state vocabulary: state constants, the `COLORS` hex table every frontend paints from, the global window-keyed tab DB + watcher pid locks — owned by the tab dispatcher, `plugins/claude_code/tabstatus.py`), `render.py` (the rendering PRIMITIVES: display-width math (`dwidth`/`dsplit`/`fit`/`term_width`), the `COL` palette + `pick` token→colour ladder, ANSI-aware wrap/gutters, `strip_ansi`, the security-critical `neutralize()`, escape-sequence unescape, section-banner emphasis, inline markdown, `BufferedStreamer`), `codefmt.py` (the bash/python source tokenizer + pretty-printer, split out of render.py: heredoc/`python -c` segment splitting, command-word marking, the `format_code` one-liner reflow, and the highlight-and-wrap `render()` the mirror paints `code` ops with; imports render's palette/width primitives one-directionally, and render.py keeps thin `format_code`/`render` delegating aliases for historical call sites), `hostpane.py` (the tool-AGNOSTIC host mirror lifecycle — `open_mirror`/`close_mirror`/`size_bar`, `decide_log_fate`/`ensure_db`/`park_db`, and `tab_host_sid` for the nested-host guard; frontend-INJECTED, so core imports no frontend — the caller passes its `fe`; shared by `plugins/claude_code/split.py` and `plugins/codex/session.py`), `slots.py` (transactional slot claim/release on the `live` table; all five palettes incl. codex), `locks.py` (pid-liveness locks — `lock_acquire`/`lock_holder`/`lock_release` on the `claims` table of an ARBITRARY caller-supplied DB path: the codex per-repo claims DB and watch lock, the OTLP receiver's per-machine singleton; borrows state.py's `_connect`/`immediate`/`pid_alive`, moved out because arbitrary-path locks were never per-session state), `audit.py` (audit writes — degrade to a spool file, never raise into a hook; the CLI entry is `bin/claude-audit.py`), `panescript.py` (the shared skeleton of the two pane-renderer ENTRY scripts, `claude-mirror.py`/`claude-scorebar.py`: the `MIRROR_LOG [WIDTH]` argv contract (`parse_argv`), the `width()` closure (`make_width`), the SIGWINCH flag-setter shape (`install_winch` — the handler body is just the caller's zero-arg flag-setter; WHAT the flag is stays per-script), the `fit` re-export, and `run_renderer` — the `__main__` crash wrapper whose "main (renderer crashed)" detail string is audit vocabulary), `paths.py` (stdlib-only leaf: the ONE owner of the `/tmp/claude-mirror-<key>.log` path format — sanitize/slug/derive/parse; every other module gets the format from here, never re-encodes it), `sessionapi.py` (the READ-SIDE session-data API — the one door for CONSUMERS of session data (the pane renderers, tooling, a future dashboard): a presentation channel of thin `core.state` delegations (`ops_after`/`stats`/`kv`/`version`/`parked`/`tab_state`/`evict` — same function objects, so re-pointing the mirror/scorebar changed nothing) plus a read model composed over the four existing stores (state DB live+parked, audit `sessions`/`streams`/`otel`/`errors`, tab DB, transcripts via `plugins.activity()`); every audit-backed query resolves the adopt `sid_chain()` first (pre-fork rows live under the OLD sid), the audit `streams` table is the keystone (src_path IS an agent's transcript, end_reason IS its final status), and there is deliberately NO new write path/events table — docs/sessionapi.md has the rejected design), `spawn.py` (THE detached audited process spawn — `spawn_detached`: DEVNULL stdio + the load-bearing `start_new_session=True` + the `A.spawn`/`A.error` rows; extracted from three byte-similar copies — `hookkit.spawn_streamer` stays as its bin/-name-resolving wrapper, and the codex launcher/watcher spawn through it), `tail.py` (the tailer skeleton: `FileTailer` byte-position line pump with the read-exactly-`size-pos` subtlety and the `consumed` checkpoint offset, `wait_for()`, `POLL_S`/`BACKSTOP_S`, and `stream_lifecycle` — the context manager owning `A.stream_start`/`stream_end` + crash-audit-then-swallow; new detached tailers go through it, which satisfies the stream-audit rule below by construction), `errwatch.py` (the audit WARNING LIGHT — the live surface over the audit `errors` table: the scorebar polls it every `POLL_S` (5s, `mode=ro`, never creates the DB) for the `▪`-row AMBER `⚠ N` chip and emits a `⚠ audit: <script>: <exception>` one-liner into the mirror per new error row, exactly once via the `errseen` kv checkpoint (its advance audited as a `state_files` row), flood-collapsed past `FLOOD_N` into one line pointing at the errors CLI; its OWN failure is audited at most once per process — the recursion guard, so a broken watcher can't feed itself), `streamfmt.py` (the shared block-shaping vocabulary of the stream RENDERERS — `cap`, the `chip`/`gutter`/`dim_gut` op shapes, the ended-footer `tok_rollup` token fragment, `file_line` — the file-op one-liner `verb(name)[ extent][ +A -R][ range]` painted by `file_fmt.py`, `substream_render.py`, and the codex patch renderer alike (per-caller extras — who-prefix, tags, ✗ mark, click-to-view hyperlink — stay caller-side), and `file_display` — the location-aware name inside those parens (bare basename under the session cwd, `✎ name` for a session-scratchpad file, dim abbreviated dir for anything else outside the project); the substream renderer and the codex stream both bind these to their own identity/colour — shared surface lives here because the dependency rule forbids codex importing claude_code).

**Claude-Code plugin modules (`plugins/claude_code/`):** `hookkit.py` (the hook-handler harness: `run()` audit-then-swallow entry, `read_payload()`, `ignore()`, `is_failure()`, `log_path()`, `spawn_streamer()` (a bin/-name wrapper over `core/spawn.py`'s `spawn_detached`, the one owner of the load-bearing `start_new_session=True` detach), and `stream_env()` — the ONE builder of the tailer's `CLAUDE_STREAM_*` env contract, used by every tailer launch site (main fg, bg, a subagent's fg); it passes the RAW command (`CLAUDE_STREAM_CMD`) and the tailer decides its own content-render mode (`stream.py _detect_render`) — launchers pass the command, never the decision (per-launcher env assembly is how the subagent fg path silently missed code colouring) — new hook handlers go through it; the `agent_id` guard stays per-handler because `monitor_fmt.py` deliberately has none), `accounting.py` (transcript token/cost accounting deduped by `message.id` via `usage_fold` — both accountants, `bump_transcript` and the substream, must share that one implementation; the `PRICES` model table; `cost_usd`; `fold_usage`), `tools.py` (Claude's built-in tool payload shapes: `parse_redirect`, `diff_counts`, `read_extent`, `edit_range`, the file-op `FILE_LABEL`/`FILE_RGB` maps), `model.py` (model/effort/context-window resolution for agents — pure functions over agent defs / meta.json / settings / the parent transcript; also `context_used`, the ONE ctx-occupancy arithmetic behind the substream ctx tag and the dashboard's saturation chips; also `claude_dirs` ancestor-`.claude/` walking — `nearest_only=True` is split.py's historical stop-at-the-nearest walk — and `settings_env`, the ONE settings.json/settings.local.json env-block layering, which split.py's mirror-width reads go through), `msgs.py` (the agent-team message tracker behind the scorebar's ✉ row, reached via `plugins.census()`), `slashcmds.py` (slash-command discovery for the web composer's "/" menu — curated CLI `BUILTINS` + the session cwd's `.claude/commands`/`skills` walk via `model.claude_dirs(env_pin=False)`, behind the `plugins.slash_commands()` fan-out), `account.py` (the subscription-account vocabulary owner — the `claude-subscription` switcher's `CLAUDE_SUBSCRIPTION_SLUG`/`LABEL` env contract + the `accounts.tsv` registry, behind `plugins.accounts()`/`plugins.account_alias()`; the dashboard's account picker/label), `statusline.py` (the status-line shim's capture half — the ONLY source of per-session 5h/7d rate limits is Claude Code's `statusLine` stdin, so `bin/claude-statusline.py` wraps the user's real status line: stash `usage`+`account` into the state DB, then delegate; tokenless, per-account, never breaks the status line), `transcript.py` (Claude transcript record shapes — the parse half of the substream's parse/paint split and the ONE owner (reader AND writer) of the transcript record shapes: `parse_line()` line→typed record, `result_text`/`input_summary`, the `agent_paths()` subagents/ layout, `set_session_title()` — the `agent-name` naming-record APPEND behind the web rename (the module's one write), and `timeline()`, the UNCAPPED drill-down read model behind `plugins.activity(sid, agent_id)` — a census-style registry fan-out; `substream_render.handle_line` dispatches on the same records, so the mirror and the drill-down can't drift; codex has the same split in `plugins/codex/rollout.py`, whose `timeline()` backs the codex activity provider), `ask_fmt.py` (the pending MODAL-DIALOG tracker behind the web ask + plan cards — AskUserQuestion `ask-pending` AND ExitPlanMode `plan-pending`: stash on PreToolUse, tool-scoped clear on answer/turn-boundary — declines fire NO hook), `relimit.py` (rate-limit ACCOUNT MIGRATION, docs/relimit.md — a main-session StopFailure carrying `error="rate_limit"` stamps the account's `limit-hit` kv (the dashboard pill's truth: the status line's frozen ~95% never reaches 100 once requests bounce) and hands off to a detached migrator that closes the tab, waits for the SessionEnd park, and relaunches `<alias> claude --resume <sid> <nudge>` under the least-used other account (`account.pick_target` over `sessionapi.account_usage`/`effective_five_hour`) — no file copying (the switcher's configs are symlink farms over the shared ~/.claude) and the adopt machinery carries the mirror history; `CLAUDE_RELIMIT=0` disables), the eight handler bodies + `dispatch.py` (the single per-event hook dispatcher behind `claude-hook.py`; sets the injected payload via `hookkit.set_payload`, and per-subsystem `audit.set_handler`, then runs each through `hookkit.run`; the tab dispatch / subagent phase / split cmd reach it via their in-process entrypoints `tabstatus.dispatch(state, d)` / `subagent_fmt.run_phase(phase)` / `split.handle(cmd, d)`) + `stream.py`/`substream.py` (the subagent streamer's LIFECYCLE: argv/env contract, model/ctx resolution, tailer spawning, cancellation signals, checkpointing, footer; its block RENDERING lives in `substream_render.py` — an import-safe `Renderer` class with `_USE`/`_RESULT` per-tool-kind dispatch tables, identity + tailer hooks injected by the lifecycle module)/`tabstatus.py`/`split.py` (`stop_fmt.py` is the `Stop`/`StopFailure` producer that folds the final-turn tail into the scoreboard, the one sanctioned Stop-time state-DB writer — distinct from the tab dispatcher, which stays `mode=ro`). **Frontend (`frontends/`):** `base.py` (the `Frontend` interface + inert stub), `kitty.py` (kitten binary lookup, silenced `kitten @` calls, `iter_windows`, `set_tab_color`, `set_tab_title` (the web rename's sticky live-tab retitle), socket resolution, the `neighbors`/`groups` geometry walk).

**Everything is keyed by `session_id`** — the state DB, pane kitty vars (`claude_mirror`/`claude_session`), sidecars — so parallel sessions never collide. Exception: background-job detection is per-project (temp slug from cwd), so two sessions in one directory can cross-talk.

## Hard-won invariants (violating these reintroduces fixed bugs)

- **Hooks must never block or fail.** Every hook path exits 0 and swallows exceptions — but every swallow site must record to the audit first (the audit `errors` table). The tab-color path writes audit rows fire-and-forget.
- **Claude Code fires NO hook on cancel/interrupt** — no Stop, nothing. Every cancellation path needs its own recovery signal: writer-liveness for commands, `meta.json` `stoppedByUser` for a killed/cancelled subagent, the **parent transcript's `tool_result`** (keyed by the agent's `meta.json` `toolUseId`) for a *rejected/abandoned* Task — which fires neither `SubagentStop` nor `stoppedByUser` — a **`StopFailure` carrying the subagent's `agent_id`** for a subagent turn that *died on an API error* (e.g. 529 — fires no `SubagentStop`; `claude-stop-fmt.py` hands it to `subagent_fmt.finalize`, the same finaliser `SubagentStop` uses; async agents can't be recovered via the parent `tool_result` because its only entry is the "launched successfully" ack), and the transcript's `[Request interrupted by user]` line for plain replies (`interrupt-watch` — which must check what FOLLOWS the line: a queued message is delivered the instant the interrupt lands, its user-prompt record right after the line is the tell, and flipping green there painted "done" over the delivered turn's think). All are **events**, never idle timeouts: cancel-before-first-hook has no signal at all and is deliberately left unhandled for a TERMINAL Esc — do not re-add an idle-timeout backstop; it false-positived on every long think. The ONE sanctioned exception is the dashboard's `escape-recheck` (docs/tab-colors.md): a WEB interrupt is itself an event we generated, so its ~2s recheck may flip the dead magenta — but only on total silence (any tab-state movement or transcript growth over the press-time baseline bails).
- **Main session only:** any hook event carrying an `agent_id` is a subagent/teammate inner call — tab dispatch ignores it, and cmd/file formatters skip it (the substream owns subagent rendering; handling both would duplicate/mis-order). **Exception:** `claude-cmd-pre.py` does tee a subagent's foreground command for live streaming (marker + `updatedInput` only — the substream still owns rendering).
- **Release slot rows *before* calling `bg-recheck`**, or the recheck sees its own row. `bg-recheck` flips only a currently-blue tab and only when no live row remains.
- **Duplicate events are real:** `SubagentStart` and `SubagentStop` can each fire more than once for background agents — both handlers guard on slot state.
- **Failures arrive on `PostToolUseFailure`, not `PostToolUse`** — any new PostToolUse hook must be wired to both or failures silently vanish.
- **SessionStart can arrive with a SCRUBBED env** — Claude Code's agents view spawns `claude daemon run`, whose hook children have no `KITTY_WINDOW_ID`/`KITTY_LISTEN_ON` (the socket still resolves via the ppid walk, so pane calls *work*, anchorless). Pane ops must anchor to `KITTY_WINDOW_ID` or the `claude_session=<sid>`-tagged window and SKIP the whole lifecycle when neither exists (a daemon agent session / headless `claude -p` has no pane) — the focused-tab fallback let a phantom session close the focused tab's mirror and open an empty one (docs/mirror-pane.md, *Anchoring, and daemon-origin SessionStarts*).
- **`--resume` AND backgrounding a session can FORK the session id** — the conversation continues under a NEW sid with no SessionStart of its own (resume: SessionStart fires under the OLD sid, `source=resume`; backgrounding: the new sid is the background-job id), so the mirror/scorebar/tab freeze on the old sid. `plugins/claude_code/adopt.py` (run first by `dispatch.py`) adopts the predecessor at the fork's first event — renames its state DB to the new sid's path (symlinks at the old paths), retags the panes, writes the missing `sessions` row — driven by the take-once `adopt_pending` note (written by `split.cmd_open` for every HOSTED session) + `sids` registry in the global tab DB. The un-adopted regression is the canned anomaly "hook traffic under a sid with no sessions row" (docs/mirror-pane.md, *Resume (and backgrounding) can FORK the sid*).
- **Replayed output must not execute: every op's text passes `render.neutralize()` at paint** (only SGR styling and OSC 8 hyperlinks survive). The ops stream carries RAW command output and the renderer replays it on every reflow — an escape sequence in some command's output re-executes forever, not once (a tee'd `@kitty-cmd` DCS scrolled the mirror to the top on every repaint, masquerading as a scroll bug). Any new paint path must route op text through `_render`/neutralize.
- Empirically-confirmed but undocumented Claude Code behaviors this repo depends on (`updatedInput` command rewriting, Ctrl+B's `backgroundTaskId`+`backgroundedByUser` payload, `stoppedByUser` in meta.json, the interrupted-transcript line) are called out in the docs/ design docs (docs/streaming.md, docs/subagents.md, docs/tab-colors.md) — check there before assuming a payload field exists or not.

## Every new feature must be audit-covered

The audit trail is only useful if it has no blind spots — a mechanism that leaves no rows is undebuggable after the fact (the no-hook-on-cancel bug class was only cracked once auditing existed). When adding or changing a feature, wire it into the audit (`core/audit.py`) **in the same commit**:

- **New hook handler** → call `A.hook_event(payload, handler=, decision=)` with a `decision` string that says what the handler chose to do and why (the decision column is what makes `hook_events` diagnostic, not just a log).
- **New detached process / tailer** → `A.stream_start(...)` on spawn and `A.stream_end(stream_id, end_reason=)` on every exit path, plus `A.spawn(...)` at the launch site (wrap the tailer in `core.tail.stream_lifecycle` and spawn via `hookkit.spawn_streamer` and all of this comes for free). A stream with `ended_at IS NULL` is an anomaly signal — don't create streams that legitimately never end.
- **New coordination/marker/sentinel file** → `A.state_file(log, path, action, content)` on write and remove; new slot kinds go through `core/slots.py` so `A.slot(...)` rows come for free.
- **New tab-state input** → record via `A.transition(...)` with `applied` and a `reason`, including (especially) the bailed/skipped paths.
- **New swallow site** → `A.error(...)` before the `except: pass` (existing invariant above — it applies to new code too).
- **New accounting/derived numbers** (scoreboard fields, cost math) → make the inputs reconstructible: either the raw source is already audited (e.g. the transcript path in `sessions`) or add rows for it. "The number is wrong" must be answerable from the DB plus the named source, as the `message.id` token-dedup bug was.
- Then extend the **`anomalies` canned queries** in `core/audit.py` if the feature has a known failure signature, and update **`.claude/skills/audit-debug/SKILL.md` in BOTH places**: the **schema table** (what the new rows/columns/kinds are) *and* the **"Known bug shapes" playbook** (which symptom should now consult that evidence, and what the tell-tale value looks like). These drift independently — the schema table alone got updated once while the playbook kept pointing at the old sources, which makes the skill *look* current while it triages blind. A new evidence source that no bug shape references is a smell: either wire it into an existing shape or add the shape it exists for. New tables/columns belong in the same migration style as the existing ones (WAL-safe, spool-file fallback, never raise into a hook).

## Style rules live in docs/styleguide.md

**Read `docs/styleguide.md` before writing or reviewing code.** It is the normative companion to `docs/architecture.md`: layout and the hyphen/underscore naming rule, the dependency rule, the single-owner vocabulary table (which module owns which shared fact — re-encoding one is a bug, several are grep-test-enforced), import-time purity (no argv/I/O/frontend/DB work at import — the dispatcher imports every handler on every event), registries over if/elif ladders, audit-before-swallow, SQL binding + `mode=ro` probe rules, named constants for tuning/protocol literals, "deliberately different — don't unify" markers, byte-identical refactoring discipline, and the test conventions (`wait_until` only, hermetic tmpdirs, seed via product APIs, env knobs). New shared facts get an owner and a row in that table in the same commit.

## docs/ is the design doc

The `docs/` directory (indexed by `docs/README.md`) is the authoritative, exhaustively-detailed record of how every mechanism works *and why the alternatives failed*. When changing behavior, update the corresponding docs/ file in the same commit — the "why not X" notes there are what prevents regressing to already-rejected designs. The repo-root README.md is a short GitHub-facing overview only; internals belong in docs/.

## Branch in an isolated worktree, merge to main, push

This is a personal repo with a linear `main` history — there is no PR/review flow — but MULTIPLE agents often work here concurrently, and an agent that edits the shared checkout tramples (and gets confused by) the others' uncommitted changes. So:

1. **Never work directly in the main checkout.** Before touching any file, create a fresh branch off the latest `main` in an **isolated git worktree** (a branch alone does NOT isolate you — everyone in the same directory shares the same working tree and sees each other's uncommitted files). Use the harness's worktree isolation (`EnterWorktree`, or `isolation: "worktree"` for subagents) when available; otherwise `git worktree add <dir> -b <branch> main` yourself.
   - **Once in a worktree, EVERY file edit must land inside that worktree — under `.claude/worktrees/<name>/`, NOT the main checkout root.** The trap: after `EnterWorktree` the shell cwd moves into the worktree, but tools that take an ABSOLUTE path (Read/Edit/Write) don't follow it — passing the main checkout's absolute path (`/…/baqylau/dashboard/…`) silently edits `main` while relative-path tools (Grep/Bash) read the worktree, so your edits and your greps diverge. Always target the worktree path (`/…/baqylau/.claude/worktrees/<name>/…`), or use paths relative to the shell cwd. When in doubt, confirm with `git rev-parse --show-toplevel` (must print the worktree, not the main checkout) before and after editing, and check `git -C <main-checkout> status` stays clean.
2. Do all work and commits on that branch, in that worktree.
3. When the change is complete and verified (code + its audit wiring + docs/ + skill docs, per the sections above, and `make test` green), **merge it into `main` and `git push`** — every time, without being asked. Keep history linear: rebase the branch onto the latest `main` first, then fast-forward merge (`git merge --ff-only`). If `main` moved underneath you, rebase and re-verify before merging.
4. Clean up: remove the worktree and delete the merged branch. Do not open PRs or leave work sitting on an unmerged branch.
5. **If the change touched the web dashboard (`dashboard/` front- or back-end, or a `core`/`plugins`/`frontends` module the server imports), RESTART the running dashboard before you consider the work done** — it is a long-running singleton that does not hot-reload (see the "does NOT hot-reload" note above). The live server runs from the MAIN checkout, so first make sure `main` there includes your just-merged commit (the main checkout must be fast-forwarded to it — a push to `origin/main` from a worktree does not touch the main checkout's working tree), then `./bin/claude-dashboard.py stop && ./bin/claude-dashboard.py start`, confirm with `status` + a `curl` against a new endpoint, and TELL THE USER to hard-reload the browser (Cmd+Shift+R) — a server restart cannot evict their cached `app.js`/`style.css`; it only bumps `BOOT_ID` so their open page toasts "dashboard updated — refresh". Skip this step only for a non-dashboard change.

(This is about *using* worktrees — the `WorktreeCreate`/`WorktreeRemove` hooks must still stay unwired, per the wiring section above.)
