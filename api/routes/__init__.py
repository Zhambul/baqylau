# api/routes/ — one router per plane of the daemon's HTTP surface:
# read (JSON GET), streams (SSE), evidence (hook deliveries), control
# (launch + gestures + terminal panes/views), application (preferences,
# drafts, presence, telemetry), files (uploads, clipboard, dictation),
# static (the SPA assets).
